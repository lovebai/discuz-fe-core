/// <reference types="react" />
import { SwitchProps } from '../interface';
export declare const SwitchWebLayout: ({ checked, defaultChecked, onChange: onCheckedChange, disabled, loading, size, color, className, style, }: SwitchProps) => JSX.Element;
