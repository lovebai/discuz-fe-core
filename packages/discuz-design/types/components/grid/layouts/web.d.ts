/// <reference types="react" />
import { GridProps } from '../interface';
export declare const GridWebLayout: ({ data, gutter, bordered, columnNum, square, iconSize, onClick, className, style, }: GridProps) => JSX.Element;
