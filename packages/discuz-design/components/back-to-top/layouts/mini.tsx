import React from 'react';
import { View } from '@tarojs/components';
import { BackToTopProps } from '../interface';

export class BackToTopMiniLayout extends React.Component<BackToTopProps> {
  render() {
    return <View>BackToTop</View>;
  }
}
